import { Editor } from '@/components/logo-forge/Editor';

export default function Home() {
  return (
    <main className="h-screen w-screen bg-neutral-100 dark:bg-neutral-900 text-foreground overflow-hidden">
      <Editor />
    </main>
  );
}
